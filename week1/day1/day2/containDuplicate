class Solution {
public:
    bool containsDuplicate(vector<int>& nums) {
        set<int>intSet;
        for(int num:nums){
            if(intSet.count(num))
            return true;
            intSet.insert(num);

        }
        return false;
        
        
    }
};